# 02 - Write the GitHub visitor README

Type: task
Status: resolved
Blocked by: 01

## Question

Rewrite root `README.md` to the destination on [GitHub visitor README for the wired delta](../map.md), using facts from [Intended clone, env, and run facts for the visitor README](01-intended-clone-env-run-facts.md).

Do: replace the Segmenter-Agent-as-tagger intro; short architecture; required PhysGaussian + PartSAM clone/weights; two envs `physgauss` + `PartSAM`; `./scripts/run_pipeline.sh`; honesty (local 3DGS, `clicks.json`, no live-ficus-quality claim); FlashSplat clone optional; optional critique subsection; Digest Dashboard; licenses; one link to `docs/agents/orientation.md`. Omit `setup_env.sh` / `physgauss_v2` from intended install. No `git push`. No `CONTEXT.md` edit. No `src/` edit.

## Answer

Root `README.md` now tells the wired visitor story: PartSAM Material Tag Tensor → Lamé → PhysGaussian MPM Solver → rasterize; required PhysGaussian `8339ed6` + PartSAM `b16d3e8`/Hub weights; envs `physgauss` + `PartSAM`; `./scripts/run_pipeline.sh`; local checkpoint + complete `clicks.json`; FlashSplat optional; mock critique CLI; Digest Dashboard; cite/licenses; one link to `docs/agents/orientation.md`. No `setup_env.sh` / `physgauss_v2`, no `CONTEXT.md` or `src/` edit, no push.
