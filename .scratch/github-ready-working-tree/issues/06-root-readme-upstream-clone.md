# 06 - Root README for upstream clones and the Phys4DGS delta

Type: task
Status: unclaimed
Blocked by: 05

## Question

Write a root `README.md` that tells a new clone how to obtain PhysGaussian (recurse-submodules into `third_party/PhysGaussian`, pin `8339ed6`), FlashSplat (`third_party/FlashSplat`, pin `3e3b147`), set `PHYSGAUSSIAN_ROOT` / `FLASHSPLAT_ROOT` if needed, and run this repo’s delta. Do not dump those upstream trees. Broader README narrative (paper abstract, full architecture) stays out of this ticket unless it fits in a short intro.
