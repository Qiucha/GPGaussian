# 10 - Point orientation at the tagging fix

Type: task
Status: resolved
Blocked by: 09

## Question

After the 5-frame bar holds, one `/writing-for-agents` pointer or correction in [docs/agents/orientation.md](../../../docs/agents/orientation.md) so Current state / Next steps do not still teach a zero-trunk live tensor as the leftover. Drop or reword the spec’s “implementing skip identity and survival in `src/`” out-of-spec line if this map has done it.

Do not add PartSAM to `CONTEXT.md`. Do not push.

## Answer

[docs/agents/orientation.md](../../../docs/agents/orientation.md): Current state reuses `material_tags.pt` only when occupancy holds; Next steps records the live ficus 5-frame bar as held and still points at [PartSAM as Material Tag Tensor source](../../partsam-as-tagger/spec.md) (reach includes occupancy). Spec: dropped “implementing skip identity and survival in `src/`” from Out of this spec; later-map bar marked done via [Implement the live ficus PartSAM tagging fix](../map.md). `CONTEXT.md` unchanged. No push.

## Comments
